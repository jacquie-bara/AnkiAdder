# AnkiAdder project context

## History actions, definition coverage and undo (2026-09-12)

- Added Delete in the history list and word detail, with a confirmation that local definitions/versions are removed while Anki cards remain. Atomic Store.remove preserves memory on write failure; generation/mutation/batch locks apply. Deleting the current word advances within the stable filtered order or returns to the list when empty.
- History detail has its own destination dropdown and live deck membership, refreshed through findNotes → notesInfo → cardsInfo in the open profile. Offline/errors show unavailable, never an empty membership claim. Adds deduplicate by exact destination deck and can create a separate copy elsewhere without moving cards. Updates apply to all matching copies and verify every note, preserving schedules, tags, decks and reverse choices. Duplicate identity remains language-pair + lemma; Anki note options now use deck scope without child decks. Pending edits remain pending when only a new deck copy was added.
- Every successful text regeneration saves an entry/model/format snapshot in textVersions. Audio replacement saves prior metadata in audioVersions and keeps existing files. Separate Undo buttons pop these persisted histories without API calls or rewinding costs/Anki links. Restoring marks linked entries pending for explicit Anki update; missing audio and renamed-word revisions fail without saving. Existing pre-feature overwritten versions cannot be recovered. Make compact now uses the same protected text regeneration path.
- Reproduced Lexin's hålla sig result: one freshness definition, plus nested adjective constructions and idioms previously discarded. Lookup now retains these. Prompts explicitly treat dictionary coverage as partial and ask for broader common constructions. The review response has mandatory senseChecks for EVERY draft, saved and supplied dictionary definition (including nested definitions), mapping to final meaning indexes. Missing checks, discarded saved meanings and collapsing distinct saved meanings reject before save. Removed draft/dictionary senses carry an explanation/warning. Saved entry is supplied to both generation and review. Two requests remain, now with 8,000 output tokens each to accommodate fuller meanings and the audit. Semantic judgments still depend on the model.
- Dictionary expressions allow natural inflections, reflexive-person changes and intervening objects, e.g. att lägga öde → lade hela byn öde. Explicit inflected inputs/full sentences retain their earlier form checks. Swedish att is normalized out of newly generated verb lemmas; existing target spellings remain fixed. Review checks construction/meaning and all translations, with regressions for lägga vs ligga öde and opposite-meaning examples.
- Validation: 55 unit tests; desktop, history actions/undo/multiple decks/deletion and batch UI checks passed with isolated profiles and mocked APIs/Anki. History test includes narrow layout, persistence and no Anki deletion. Live testing used the existing packaged safeStorage identity read-only to unlock the saved key: 18 paid text requests across three rounds (no speech or real Anki writes). Read every returned definition and example; initial rounds exposed duplicates, wrong-language definitions and mismatched examples, prompting review refinements. Final live outputs: 8 senses for new hålla sig; regeneration of the user's three prior senses returned 9, including staying, state/self-control, conduct, avoidance and freshness, plus informed/reserved/noticed/bodily-urge uses; att lägga öde returned grammatical lade constructions with normalized lemma. Results are in test-output/coverage-live-rRfQ8f. Limited samples do not guarantee future model correctness.
- Rebuilt unsigned Apple Silicon DMG/ZIP with CSC_IDENTITY_AUTO_DISCOVERY=false npm run dist:mac (version stays 1.3.0); packaged startup and dedicated settings autosave checks passed. User changes that predated this session remain in the working tree, including fixes.md.

Updated 2026-09-11 for version 1.3.0. Workspace: C:\dev\AnkiAdder. No git repository or AGENTS.md was present during this session.

## History navigation and text-only regeneration (2026-09-12)

- User explicitly allows natural occasional `hen` alongside `han`/`hon`; both generation and review prompts now say so, using singular `they` in English where appropriate.
- History entries now open inside Your words, with Previous/Next buttons, a position counter, and Left/Right keyboard navigation through a snapshot of the filtered result order. No wrapping at boundaries; edits/regeneration do not change the active browsing order. Back returns to the same search. Keyboard navigation is ignored during operations, in inputs/contenteditable elements, with modifier keys, and while a dialog is open. The shared preview and cost display move between create/history instead of duplicating controls.
- Added `word:regenerate-text` IPC / `regenerateText(id)` bridge and Regenerate text only below each preview. Uses original `entry.word`, fixed saved lemma, original language pair, and current selected model. Keeps the same history ID, original creation time, audio metadata/file, pronunciation text and Anki link. Separate generation/review still run; cumulative text receipts retain prior charges. A changed lemma is rejected. No speech request or automatic Anki write; linked entries get `pendingAnkiChanges` for explicit Update existing card. Cancellation/failure preserves the original record. Existing settings and user changes in `fixes.md` remain intact.
- `records.regenerateRecord` handles atomic replacement metadata/costs; main process holds the generation lock through saving. UI shows progress and cancellation. `npm run test:history` covers arrows/buttons/filtering, stable position, edit/modifier exclusions, original-language requests, audio bytes/IPA/identity and cost retention, failures/cancellation/concurrency, explicit Anki update and reload using temporary profiles and mocked services. No live API calls or real Anki writes for this follow-up.
- Validation: 48 unit tests plus desktop, batch, dedicated history UI (including narrow viewport) and packaged startup passed. Rebuilt unsigned Apple Silicon DMG/ZIP in `release` with `CSC_IDENTITY_AUTO_DISCOVERY=false npm run dist:mac`, version remains 1.3.0. History screenshot: `test-output/history-navigation.png` (mock data).

## Grammar-review follow-up (2026-09-12)

- User showed a real failure: `Hon urdrucken sitt glas innan hon gick hem.` The input-preservation check passed even though a participle was used as a finite verb. Presence checks cannot establish grammatical correctness. Swedish `det` can refer to `barnet`, but examples now prefer clear personal references and avoid English `it` for a human child.
- New entries use TWO sequential text requests with the selected model: generation, then a separate language editor that receives the original input, dictionary evidence and draft. It corrects grammatical roles, finite verbs/auxiliaries, agreement, pronoun references, contextual usefulness and translations. Added explicit participle/finite-past/supine distinctions and agreeing-subject examples. If preserving the input requires ungrammatical text, return an explanation rather than force it into a sentence.
- Review must complete and validate before saving, speech or Anki addition. Failure never falls back to the unreviewed draft. Original and review warnings are retained and block automatic addition. Phrase-preservation checks run on the reviewed result. Each text request retains the 4,000-token limit; review adds time and API usage. Main process stores both receipts via `pricing.combine`, and the illustrative estimate includes both requests. Historical receipts/manual edits remain unchanged.
- Regression coverage includes the screenshot draft being replaced by review output, review failures/refusals/malformed responses, cancellation between requests, warning retention, combined costs and an Electron check that review failure blocks history/audio/automatic Anki writes. Mocked tests verify the pipeline, not the model's grammatical competence.
- Live testing with the saved Luna model exposed awkward English translations and confusion between the particle verb and prepositional `drink from`; refined review instructions and added a Lexin lookup of the extracted lemma when the whole phrase has no exact match. Final live screenshot review returned `Flaskan var urdrucken, så hon öppnade en ny åt gästerna.` / `The bottle was empty, so she opened a new one for the guests.` and `När hon hade druckit ur sitt glas gick hon hem.` / `After she had finished her drink, she went home.` Final phrase test preserved `drack ur sitt kaffe` with one correct particle-verb sense. These are limited live samples, not a guarantee of future correctness.
- Live checks used the packaged app's safeStorage identity to unlock the existing key read-only (development Electron could not decrypt it); made 10 paid text requests total across initial and refined samples, no speech or real Anki writes, and saved results only under isolated `test-output/language-live-*` profiles. Unit tests now total 47; desktop/batch and packaged startup checks passed. Mac DMG/ZIP rebuilt with the review workflow, still version 1.3.0 and unsigned.

## Dictionary, phrases and examples (2026-09-12)

- Investigated `urdrucken` using Lexin's live public JSON service: it is the perfect participle of `dricka ur`, listed under `dricker ur`. The previous generator never queried a dictionary; unknown-input rejection came from the model's response.
- Added `src/lexin.cjs`: Swedish/Svenska/sv/swe inputs receive a five-second, cancellable HTTPS dictionary lookup before the existing single text request. Only matching headwords/inflections provide definitions and forms; ignore spelling suggestions and unrelated results. No match, offline, HTTP or JSON failure falls back to model generation. No secrets are sent to Lexin. README documents this new data flow.
- Generation now identifies a phrase's meaningful lexical verb, retains particles/reflexive components, and requests the full original phrase in an example without changing tense/conjugation/word order. Runtime checks enforce phrase/inflected-input preservation (case, whitespace and final punctuation may differ), including scripts without word spaces. The original input remains in `entry.word`; lemma still controls card heading, audio and duplicate identity. Legacy saved entries and manual edits retain their prior validation.
- Examples now request meaningful situations/reasons/consequences, generally 8–18 words, with an explicit semantic self-check and contrasting weak/improved examples. Removed the old 4–8-word and 35–45-word-per-sense instructions. Sentence/translation caps are now 280/340 characters to allow up to 200-character input plus context. Still two examples per meaning, six principal forms, no fixed sense cap, 4,000 output tokens. Quality remains model-dependent; mocked tests do not measure live model language quality.
- Added a recorded Lexin regression fixture and tests for grounding, suggestions, outages, cancellation, phrase preservation, conjugation changes, long sentences, and Japanese. Existing user edits to `fixes.md` are preserved. Existing history/Anki text is not automatically rewritten.
- Validation: 42 unit tests, desktop UI smoke, batch UI, live read-only Lexin lookup and packaged Mac startup passed. Rebuilt unsigned Apple Silicon DMG/ZIP in `release` with `CSC_IDENTITY_AUTO_DISCOVERY=false npm run dist:mac` (version remains 1.3.0). No paid model requests, real user-data changes or Anki writes were made during testing.

## Mac follow-up (2026-09-11)

### Unattended word batches (2026-09-11)

- Added **Add many words**: one word/phrase per line (up to 1,000), exact repeated/blank lines removed, destination deck selection, automatic generation/pronunciation/Anki addition independent of the single-word auto-add setting. Uses a snapshot of saved settings; no setting changes during processing. Existing notes are skipped and uncertain entries remain drafts for review. Per-word errors do not stop subsequent words.
- `src/batch.cjs` owns the sequential main-process queue and atomic `batch.json` checkpoints. Stop finishes the current word; resume uses original settings. Restarted interrupted queues load paused. Retry failed words reuses history records by `batchItemId`, avoiding paid text regeneration after audio/Anki failures. New batches replace the previous queue summary; history entries remain.
- Added narrow batch IPC methods in preload/main, main-process idle-sleep prevention for active batches, progress polling that survives renderer navigation/reload, per-word results and view-entry links. Closing a window lets the batch finish; explicitly quitting requires resume after restart. Keep Anki on the same profile throughout.
- `npm run test:batch` covers mock automatic Anki/audio additions, preflight connectivity, failure continuation/retry, duplicates, navigation/reload, settings lock and stop/restart/resume. `test/batch.test.cjs` covers parsing, sequential processing, stop/persistence, interrupted recovery and preflight/overlap failures. All tests use temporary profiles and mock APIs; no real API calls or collection writes.

Current workspace is `/Users/yiwang/Projects/ankiadder/AnkiAdder` on macOS 15, Apple Silicon, with git at the parent directory. Use the current shell/environment rather than the historical Windows workflow notes below. Existing user formatting changes in `src/ui/index.html` were preserved.

- Removed the eyebrow, title and introductory paragraph from Add a word, Your words and Settings. Kept functional labels/help and restored a screen-reader label for the word input.
- Shared card CSS now uses transparent backgrounds in both light and night mode. The next Anki add/update refreshes the shared note-type CSS for existing cards; background changes do not require regenerating entries.
- Investigated the real cached `skum` MP3 read-only: 0.36 seconds, approximately -90 dB peak, effectively silence. Anki's media copy has the identical SHA256. Mac MP3 playback works with an audible fixture; this recording itself explains the reported silence in both apps.
- Added `src/ui/audio.js` to decode/check playback for silence or corrupt audio and display an actionable error. Regenerate pronunciation makes one explicit paid speech request without regenerating text. It saves under a fresh filename, preserves prior files on failure, accumulates pronunciation receipts, and marks linked notes for Update existing card. Playback after reload uses the replacement filename without requesting speech again. Original live user files were not modified and no real API calls were made during verification.
- UI tests use a locally generated tone (muted) for playback and a silent MP3 for rejection. Added regeneration failure/retry, receipt accumulation, persistence, no text regeneration and Anki audio upload checks, plus light/night transparency checks. 26 unit tests, Mac UI smoke and dedicated autosave checks passed. Packaged smoke now supports Mac and no longer depends on the removed heading.
- Rebuilt the Apple Silicon DMG/ZIP in `release` (version remains 1.3.0) and passed the packaged Mac startup check with a temporary profile. Build with `CSC_IDENTITY_AUTO_DISCOVERY=false npm run dist:mac` to avoid automatically selecting a local signing certificate; the normal build stalled on certificate signing. No Developer ID signing/notarization was performed.

Historical Windows context follows.

## Product and user preferences

Cross-platform desktop app: type a word, generate a compact vocabulary entry using the user's OpenAI API key, review, and add to the running local Anki desktop app through AnkiConnect. Default text model is gpt-5.6-luna, editable in Settings. Default languages Spanish / English; preserve the user's saved settings. User studies multiple languages, including Swedish and Japanese.

Match the user's reference: centered word/part of speech, brief numbered definition summary, highlighted single line of principal forms, numbered detailed meanings with exactly two short translated examples each. Examples must appear only under each meaning, never repeated in its heading. Legacy `usage` fields caused duplicate examples; rendering now ignores usage and new generation requests it empty. No citations like [1,2]. Aim for one desktop screen, but allow scrolling for words with many common meanings. No three-meaning cap: merge near synonyms, omit rare senses, keep descriptions/examples short. At most six principal forms, not full conjugation tables. Output budget remains 4,000 tokens with low reasoning for Luna.

Pronunciation uses gpt-4o-mini-tts, default Marin; optional toggle on Add a word and in Settings. Cache MP3s and attach to Anki for offline playback/sync. Separate text/pronunciation cost estimates in USD per entry; display toggle in Settings. Saved receipts reflect reported usage and bundled rates, not invoices. Manual edits make no LLM call.

All settings autosave after 600 ms of inactivity, with visible save status; navigation/generation flush pending changes. Invalid fields retain previously saved settings and show an error. No Save settings button. Deck selection is on Add a word, uses a full dropdown, supports nested/empty/Unicode deck names, manual refresh and new deck names. AnkiConnect exposes only the currently open profile: switch profiles in Anki and refresh. A live read-only check this session reported three decks; the old autocomplete could filter by the existing typed name. Never change the user's Anki profile automatically.

Every app result/history entry has Edit. Structured form supports word, lemma, pronunciation text, grammar, meanings with two examples, forms, notes/coverage/warnings. Save changes persists locally; Save & update Anki / Update existing card explicitly updates Anki. Updates preserve schedules, deck, tags, reverse choice. Renamed lemmas follow original identity, reject collisions, and clear stale pronunciation. No audio request during an edited note update; user may explicitly generate new audio. Does not import arbitrary cards from Anki.

## Architecture

- `src/main.cjs`: Electron lifecycle, IPC, generation, local editing, audio, Anki actions. OS window/dock icons. Hidden test launch supported with --smoke-test.
- `src/preload.cjs`: narrow promise API. Renderer is sandboxed/context-isolated, no Node/network; only trusted top-level local frame may invoke IPC.
- `src/core.cjs`: settings and entry validation, strict Responses JSON schema/prompt, AnkiConnect, note type/templates/CSS. Model is `AnkiAdder Vocabulary v1`; duplicate identity is SHA256 of source language, translation language and normalized lemma. Explicit updates verify notesInfo because Anki can silently refuse while its editor is open. Updates also refresh app-owned shared note templates/styles.
- `src/records.cjs`: local edit transformation; original Anki identity migration, pending edits and stale audio invalidation. Costs remain historical.
- `src/store.cjs`: atomic settings/history JSON writes, safeStorage encryption (no plaintext fallback), secret-free public settings, unbounded history retention. Data normally `%APPDATA%/anki-adder` or `~/Library/Application Support/anki-adder`; don't print keys or overwrite real user data during testing.
- `src/audio.cjs`: speech SSE MP3 assembly + usage, validated cache paths, binary fallback with unavailable price if usage missing.
- `src/ui/index.html`, `app.js`: pages, autosave, deck picker, preview/history and API cost display. `editor.js`: native modal structured editor.
- `src/ui/card.js` + `card.css`: shared preview/Anki rendering, escaped plain text, citation stripping, numbered meanings and single form line. `compact.css`, `costs.css`, `styles.css`: app layout.
- `src/ui/pricing.js`: rates checked 2026-09-11, cached-input discounts, usage receipts, illustrative pre-generation estimates. Unknown usage/models/tiers => unavailable. Luna input/cached/output $0.20/$0.02/$1.20 per million; Mini TTS text/audio $0.60/$12. Other supported model rates in file. Prices require future rechecking against official OpenAI docs.
- `src/assets`: native SVG logo plus PNG/ICO/ICNS; `scripts/icons.cjs` uses sharp. No image generation needed for the existing vector icon.

## Run, test, package

Node.js 24 LTS. `npm ci`, `npm start`. Electron 44.3.0 / electron-builder 26.15.3 / Playwright 1.63.0; no production npm dependencies.

- `npm test`: core, audio, pricing, editing, storage tests using mock APIs.
- `npm run test:ui`: hidden Electron with isolated temporary profile and mock OpenAI/Anki calls, actual muted MP3 decoding, compact layout, full deck picker, autosave, history editing/reload, Anki updates, speech/offline recovery, costs/toggles. Screenshots in test-output. No API charges or real collection mutations.
- `node test/settings-ui.cjs`: immediate navigation and window-close autosave, invalid-field preservation. Electron manages beforeunload cancellation; Playwright's browser dialog fallback must be suppressed in this test.
- `npm run dist:win`: NSIS installer with Desktop/Start menu icon in release.
- `node test/packaged.cjs`: isolated hidden installed-bundle startup check.
- `python scripts/mac-kit.py`: source build-kit ZIP including this file. `Build Mac App.command` inside has Unix executable permissions and LF endings; on Mac with Node24 it installs dependencies and builds a DMG/ZIP.
- `.github/workflows/build.yml`: Windows x64 + Mac arm64/x64 builds on native hosts, artifact uploads only; requires pushing this project to GitHub to use.

Release targets: `release/AnkiAdder Setup 1.3.0.exe`, `release/AnkiAdder-Mac-Build-Kit-1.3.0.zip`. Older version artifacts may also exist; link the newest. Mac native binaries cannot be built on this Windows machine (electron-builder explicitly rejects it); don't claim a tested Mac binary. Builds are unsigned. Normal Windows operation after installing needs no terminal; Mac normal operation likewise after native build/install.

## Validation and remaining practical limits

25 unit tests passed for 1.3 changes. Desktop tests passed: three-meaning preview fits the 787px viewport (about 778px bottom), six examples, editable history persistence without additional paid requests, Anki field updates, deck selection and automatic settings saving. Dedicated autosave tests passed for immediate navigation, invalid-input preservation and closing before debounce expires. Windows 1.3.0 installer build and packaged app startup passed. Live Anki calls this session were read-only deck discovery; LLM and Anki mutations use mocks, so factual language accuracy/live billing were not evaluated. Mac runtime must be checked on a Mac.

Existing Anki cards store their old generated HTML until explicitly updated; changing preview rendering alone does not rewrite all historical Anki notes. Edit local history then Update existing card to apply fixes. Only the open Anki profile is accessible; updates of linked notes fail clearly if absent there. Ordinary Add uses the currently chosen deck; Update keeps the original deck. Avoid regenerating just to fix text: manual editing is free. More than three common senses are supported but completion can still hit the bounded output budget; incomplete responses produce no card rather than silently truncating.

## Session workflow notes

Use PowerShell-native file operations. Prefer rg. Use UTF-8 when reading source (PowerShell's legacy default display can look like mojibake). Use apply_patch for source changes; multiple Update blocks for the same file in one patch are rejected. Scripts passed via `node -e` can lose quotes in this environment; use a file for substantial test scripts. No subagents unless explicitly requested. Continue authorized implementation/testing/build work without repeated approval requests. Never send messages externally without user authorization.
